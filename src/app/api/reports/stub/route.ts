import { NextResponse } from "next/server";

/** Placeholder : brancher ici pdfmake / @react-pdf avec RTL arabe (rapports CDC). */
export async function GET() {
  return NextResponse.json({
    ok: true,
    message: "PDF multi-langues RTL — à intégrer (comparaison, photo IA, OBD2, rapport final).",
  });
}
